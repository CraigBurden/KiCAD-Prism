diff --git a/src/ecad-viewer/ecad_viewer.ts b/src/ecad-viewer/ecad_viewer.ts
index d48d9d2..c827ffb 100644
--- a/src/ecad-viewer/ecad_viewer.ts
+++ b/src/ecad-viewer/ecad_viewer.ts
@@ -9,7 +9,7 @@ import {
 } from "../base/web-components";
 import { KCUIElement } from "../kc-ui";
 import kc_ui_styles from "../kc-ui/kc-ui.css";
-import { AssertType, Project } from "../kicanvas/project";
+import { AssertType, Project, type ProjectPage } from "../kicanvas/project";
 import { type EcadBlob, type EcadSources } from "../kicanvas/services/vfs";
 import { KCBoardAppElement } from "../kicanvas/elements/kc-board/app";
 import { KCSchematicAppElement } from "../kicanvas/elements/kc-schematic/app";
@@ -29,6 +29,7 @@ import {
     TabMenuClickEvent,
     TabMenuVisibleChangeEvent,
     type CrossProbeContext,
+    type EcadSemanticSelectionDetail,
     type CrossProbeFailureReason,
     type CrossProbeRequest,
     type CrossProbeResult,
@@ -58,6 +59,7 @@ type SelectionDetail = {
     item: unknown;
     previous: unknown;
     sourceContext?: CrossProbeContext;
+    semantic?: EcadSemanticSelectionDetail;
 };
 
 export class ECadViewer extends KCUIElement implements InputContainer {
@@ -160,6 +162,7 @@ export class ECadViewer extends KCUIElement implements InputContainer {
     #viewers_container: HTMLDivElement;
     #is_full_screen = false;
     #cross_probe_enabled = false;
+    #active_schematic_project_path?: string;
     get project() {
         return this.#project;
     }
@@ -190,6 +193,10 @@ export class ECadViewer extends KCUIElement implements InputContainer {
         }
     }
 
+    public get showSelectionPanel(): boolean {
+        return this.#boolean_attribute("show-selection-panel", true);
+    }
+
     @attribute({ type: Boolean })
     public loading: boolean;
 
@@ -284,16 +291,109 @@ export class ECadViewer extends KCUIElement implements InputContainer {
 
         this.setActiveTab(TabKind.sch);
 
-        const project = this.#project;
-        const sch = project.file_by_name(pageId);
-        if (sch) {
-            this.#schematic_app.viewer.load(sch as any);
+        const page = this.#project.pages.find((candidate) =>
+            candidate.project_path === pageId
+            || candidate.filename === pageId
+            || candidate.name === pageId
+            || candidate.page === pageId,
+        );
+        if (page) {
+            this.#load_schematic_page(page);
             return;
         }
 
         console.warn(`switchPage: Could not find page with ID ${pageId}`);
     }
 
+    public navigateSchematicPage(direction: -1 | 1): boolean {
+        if (!this.#schematic_app) return false;
+        const pages = this.#project.pages;
+        if (!pages.length) return false;
+        const current = this.#active_schematic_page();
+        const active_index = pages.findIndex((page) => page.project_path === current?.project_path);
+        const current_index = active_index >= 0 ? active_index : 0;
+        const next_index = (current_index + direction + pages.length) % pages.length;
+        return this.#load_schematic_page(pages[next_index]!);
+    }
+
+    public navigateSchematicParent(): boolean {
+        if (!this.#schematic_app) return false;
+        const pages = this.#project.pages;
+        const current = this.#active_schematic_page();
+        if (!current) return false;
+        const segments = current.sheet_path.split("/").filter(Boolean);
+        const parent_path = segments.length > 1
+            ? `/${segments.slice(0, -1).join("/")}`
+            : "";
+        const parent = pages.find((page) => page.sheet_path === parent_path)
+            ?? pages
+            .filter((page) =>
+                page.project_path !== current.project_path
+                && page.sheet_path.length < current.sheet_path.length
+                && current.sheet_path.startsWith(`${page.sheet_path}/`),
+            )
+            .sort((a, b) => b.sheet_path.length - a.sheet_path.length)[0];
+        if (!parent) return false;
+        return this.#load_schematic_page(parent);
+    }
+
+    public getActiveSchematicPage(): {
+        projectPath: string;
+        sheetPath: string;
+        filename: string;
+        name?: string;
+        page?: string;
+    } | null {
+        const active = this.#active_schematic_page();
+        return active ? {
+            projectPath: active.project_path,
+            sheetPath: active.sheet_path,
+            filename: active.filename,
+            name: active.name,
+            page: active.page,
+        } : null;
+    }
+
+    #active_schematic_page(): ProjectPage | undefined {
+        const tracked = this.#project.pages.find((page) =>
+            page.project_path === this.#active_schematic_project_path,
+        );
+        if (tracked) return tracked;
+        const filename = (this.#safe_viewer(this.#schematic_app) as any)?.schematic?.filename;
+        return this.#project.pages
+            .filter((page) => !filename || page.filename === filename)
+            .sort((a, b) => this.#page_depth(a) - this.#page_depth(b))[0];
+    }
+
+    #page_depth(page: ProjectPage): number {
+        return page.sheet_path.split("/").filter(Boolean).length;
+    }
+
+    #sync_active_schematic_page(page: ProjectPage | undefined): void {
+        this.setAttribute("schematic-page-count", String(this.#project.pages.length));
+        if (!page) {
+            this.removeAttribute("active-sheet-path");
+            this.removeAttribute("active-project-path");
+            this.removeAttribute("active-page-number");
+            return;
+        }
+        this.setAttribute("active-sheet-path", page.sheet_path);
+        this.setAttribute("active-project-path", page.project_path);
+        if (page.page) this.setAttribute("active-page-number", page.page);
+        else this.removeAttribute("active-page-number");
+    }
+
+    #load_schematic_page(page: ProjectPage): boolean {
+        const document = page.document;
+        if (!document) return false;
+        this.setActiveTab(TabKind.sch);
+        this.#active_schematic_project_path = page.project_path;
+        this.#sync_active_schematic_page(page);
+        this.#project.activate_sch(page.project_path);
+        this.#schematic_app.viewer.load(document as any);
+        return true;
+    }
+
     public setActiveTab(tab: TabKind | string): boolean {
         const parsed_tab = this.#normalize_tab(tab);
         if (!parsed_tab || !this.#tab_contents[parsed_tab]) {
@@ -341,6 +441,13 @@ export class ECadViewer extends KCUIElement implements InputContainer {
         return final_result;
     }
 
+    public clearCrossProbe(): void {
+        const board_viewer = this.#safe_viewer(this.#board_app) as any;
+        const schematic_viewer = this.#safe_viewer(this.#schematic_app) as any;
+        board_viewer?.clear_highlight?.();
+        schematic_viewer?.clear_highlight?.();
+    }
+
     /**
      * Get screen coordinates from world coordinates
      */
@@ -736,12 +843,95 @@ export class ECadViewer extends KCUIElement implements InputContainer {
         return find_designator(item);
     }
 
+    #selection_semantic_detail(
+        item: unknown,
+        source_context: "SCH" | "PCB",
+    ): EcadSemanticSelectionDetail {
+        const record = item && typeof item === "object"
+            ? (item as Record<string, any>)
+            : {};
+        const nested_value = (keys: string[], value: unknown = item, depth = 0): unknown => {
+            if (!value || typeof value !== "object" || depth > 4) return undefined;
+            const entry = value as Record<string, any>;
+            for (const key of keys) {
+                const candidate = entry[key];
+                if (candidate !== undefined && candidate !== null && candidate !== "") return candidate;
+            }
+            for (const child of [entry["parent"], entry["item"], entry["context"], entry["footprint"]]) {
+                const candidate = nested_value(keys, child, depth + 1);
+                if (candidate !== undefined) return candidate;
+            }
+            return undefined;
+        };
+        const string_value = (value: unknown): string | undefined => {
+            if (typeof value !== "string" && typeof value !== "number") return undefined;
+            const normalized = String(value).trim();
+            return normalized || undefined;
+        };
+        const viewer = source_context === "PCB"
+            ? (this.#safe_viewer(this.#board_app) as any)
+            : (this.#safe_viewer(this.#schematic_app) as any);
+        const raw_net = record["net"];
+        const net_record = raw_net && typeof raw_net === "object"
+            ? (raw_net as Record<string, unknown>)
+            : null;
+        const net_code = typeof raw_net === "number"
+            ? raw_net
+            : typeof net_record?.["number"] === "number"
+                ? net_record["number"]
+                : undefined;
+        const net = string_value(
+            net_record?.["name"]
+            ?? record["net_name"]
+            ?? record["netName"]
+            ?? (typeof raw_net === "string" ? raw_net : undefined)
+            ?? (net_code !== undefined ? viewer?.board?.getNetName?.(net_code) : undefined),
+        );
+        const constructor_name = (() => {
+            const find_constructor = (value: unknown, depth = 0): string | undefined => {
+                if (!value || typeof value !== "object" || depth > 4) return undefined;
+                const entry = value as Record<string, any>;
+                const name = string_value(entry.constructor?.name);
+                if (name && name !== "Object" && name !== "Array" && name.length > 1) return name;
+                for (const child of [entry["item"], entry["parent"], entry["context"], entry["footprint"]]) {
+                    const nested = find_constructor(child, depth + 1);
+                    if (nested) return nested;
+                }
+                return undefined;
+            };
+            return find_constructor(item);
+        })();
+        const reference = this.#extract_designator_from_selection(item) ?? undefined;
+        const pin = string_value(nested_value(["pin", "pinNumber", "number", "padNumber"]));
+        const explicit_item_type = string_value(nested_value(["typeId", "itemType", "kind"]));
+        const inferred_item_type = explicit_item_type
+            ?? constructor_name
+            ?? (reference && pin ? (source_context === "PCB" ? "pad" : "pin") : undefined)
+            ?? (reference ? (source_context === "PCB" ? "footprint" : "symbol") : undefined)
+            ?? (net ? "net" : undefined)
+            ?? (source_context === "PCB" ? "pcb-item" : "schematic-item");
+        return {
+            sourceContext: source_context,
+            itemType: inferred_item_type,
+            uuid: string_value(nested_value(["uuid", "tstamp", "source_uuid"])),
+            crossIndex: string_value(nested_value(["cross_index", "crossIndex"])),
+            reference,
+            pin,
+            net,
+            netCode: net_code,
+            sheet: source_context === "SCH" ? string_value(viewer?.schematic?.filename) : undefined,
+            page: source_context === "SCH" ? string_value(viewer?.schematic?.filename) : undefined,
+            layer: string_value(record["layer"]),
+            rawItem: item,
+        };
+    }
+
     #can_auto_cross_probe(): boolean {
         return this.#cross_probe_enabled && this.has_sch && this.has_pcb;
     }
 
     #auto_cross_probe_from_selection(
-        source_context: CrossProbeContext,
+        source_context: "SCH" | "PCB",
         detail: SelectionDetail,
     ): void {
         if (!this.#can_auto_cross_probe()) return;
@@ -761,19 +951,20 @@ export class ECadViewer extends KCUIElement implements InputContainer {
 
     #relay_selection_event(
         event: Event,
-        source_context: CrossProbeContext,
+        source_context: "SCH" | "PCB",
     ): void {
         const detail = (event as CustomEvent<SelectionDetail>).detail;
         const normalized: SelectionDetail = {
             item: detail?.item ?? null,
             previous: detail?.previous ?? null,
             sourceContext: source_context,
+            semantic: this.#selection_semantic_detail(detail?.item ?? null, source_context),
         };
         this.dispatchEvent(new KiCanvasSelectEvent(normalized));
         this.#auto_cross_probe_from_selection(source_context, normalized);
     }
 
-    #resolve_cross_probe_target_context(source: CrossProbeContext): CrossProbeContext {
+    #resolve_cross_probe_target_context(source: CrossProbeContext): "SCH" | "PCB" {
         return source === "SCH" ? "PCB" : "SCH";
     }
 
@@ -795,6 +986,19 @@ export class ECadViewer extends KCUIElement implements InputContainer {
         const viewer = this.#safe_viewer(this.#board_app) as any;
         if (!viewer) return false;
 
+        if (hint.net || hint.netCode !== undefined) {
+            const net_number = viewer?.board?.findNetNumber?.(hint.netCode ?? hint.net);
+            if (net_number === null || net_number === undefined) return false;
+            this.setActiveTab(TabKind.pcb);
+            viewer.focus_net?.(net_number);
+            return true;
+        }
+
+        if (hint.uuid && viewer.focus_uuid?.(hint.uuid)) {
+            this.setActiveTab(TabKind.pcb);
+            return true;
+        }
+
         const fp = this.#find_cross_probe_footprint(viewer, hint);
         if (!fp) return false;
 
@@ -809,8 +1013,13 @@ export class ECadViewer extends KCUIElement implements InputContainer {
         hint: CrossProbeTargetHint,
     ): any | null {
         const board = viewer?.board;
-        const key = hint.uuid || hint.designator || "";
-        return key ? board?.find_footprint?.(key) ?? null : null;
+        if (hint.uuid) {
+            const by_uuid = board?.find_footprint?.(hint.uuid);
+            if (by_uuid) return by_uuid;
+        }
+        return hint.designator
+            ? board?.find_footprint?.(hint.designator) ?? null
+            : null;
     }
 
     #focus_pcb_cross_probe(viewer: any, fp: any): void {
@@ -848,6 +1057,15 @@ export class ECadViewer extends KCUIElement implements InputContainer {
     }
 
     #focus_schematic_cross_probe(viewer: any, hint: CrossProbeTargetHint): boolean {
+        const source_uuids = hint.uuids?.filter(Boolean) ?? [];
+        if (source_uuids.length) {
+            this.setActiveTab(TabKind.sch);
+            return Boolean(viewer.highlight_uuids?.(source_uuids, hint.mode !== "hover"));
+        }
+        if (hint.pin && hint.uuid) {
+            this.setActiveTab(TabKind.sch);
+            return Boolean(viewer.highlight_uuids?.([hint.uuid], hint.mode !== "hover"));
+        }
         const sch = viewer?.schematic;
         const key = hint.uuid || hint.designator || "";
         const symbol = key ? sch?.find_symbol?.(key) : null;
@@ -871,6 +1089,8 @@ export class ECadViewer extends KCUIElement implements InputContainer {
             designator: request.designator?.trim(),
             net: request.net?.trim(),
             crossIndex: request.crossIndex?.trim(),
+            uuid: request.uuid?.trim(),
+            uuids: request.uuids?.map((uuid) => uuid.trim()).filter(Boolean),
         };
     }
 
@@ -916,18 +1136,33 @@ export class ECadViewer extends KCUIElement implements InputContainer {
         }
 
         if (request.kind === "uuid") {
-            const net_item = this.#project.find_net_item(request.value);
-            if (!net_item) {
-                return this.#unresolved_probe(request, "uuid-not-found");
-            }
             return {
                 resolved: true,
                 request,
                 targetHint: {
                     context: target_context,
-                    uuid: request.value,
-                    net: net_item.name,
-                    sheet: net_item.sheet_name,
+                    uuid: request.uuid || request.value,
+                    uuids: request.uuids,
+                    designator: request.designator,
+                    pin: request.pin,
+                    sheet: request.sheet,
+                    mode: request.mode,
+                },
+            };
+        }
+
+        if (request.kind === "net") {
+            return {
+                resolved: true,
+                request,
+                targetHint: {
+                    context: target_context,
+                    net: request.net || request.value,
+                    netCode: request.netCode,
+                    uuid: request.uuid,
+                    uuids: request.uuids,
+                    sheet: request.sheet,
+                    mode: request.mode,
                 },
             };
         }
@@ -988,6 +1223,7 @@ export class ECadViewer extends KCUIElement implements InputContainer {
         if (this.has_pcb) {
             this.#board_app = html`<kc-board-app>
             </kc-board-app>` as KCBoardAppElement;
+            this.#board_app.hideSelectionPanel = !this.showSelectionPanel;
             this.#embed_to_tab(this.#board_app, TabKind.pcb);
             this.#board_app.addEventListener(KiCanvasSelectEvent.type, (e) => {
                 this.#relay_selection_event(e, "PCB");
@@ -1012,12 +1248,34 @@ export class ECadViewer extends KCUIElement implements InputContainer {
         if (this.has_sch) {
             this.#schematic_app = html`<kc-schematic-app>
             </kc-schematic-app>` as KCSchematicAppElement;
+            this.#schematic_app.hideSelectionPanel = !this.showSelectionPanel;
             this.#tab_contents[TabKind.sch] = this.#schematic_app;
             this.#embed_to_tab(this.#schematic_app, TabKind.sch);
             this.#schematic_app.addEventListener(KiCanvasSelectEvent.type, (e) => {
                 this.#relay_selection_event(e, "SCH");
             });
             this.#schematic_app.addEventListener(SheetLoadEvent.type, (e) => {
+                const filename = typeof e.detail === "string"
+                    ? e.detail
+                    : (e.detail as any)?.filename;
+                const tracked = this.#active_schematic_page();
+                if (!tracked || (filename && tracked.filename !== filename)) {
+                    const candidates = this.#project.pages.filter((page) => page.filename === filename);
+                    const activated = candidates.find((page) =>
+                        page.project_path === this.#project.active_sch_name,
+                    );
+                    const direct_child = tracked
+                        ? candidates.find((page) =>
+                            this.#page_depth(page) === this.#page_depth(tracked) + 1
+                            && page.sheet_path.startsWith(`${tracked.sheet_path}/`),
+                        )
+                        : undefined;
+                    const resolved = activated
+                        ?? direct_child
+                        ?? candidates.sort((a, b) => this.#page_depth(a) - this.#page_depth(b))[0];
+                    this.#active_schematic_project_path = resolved?.project_path;
+                }
+                this.#sync_active_schematic_page(this.#active_schematic_page());
                 if (this.#tab_header) {
                     this.#tab_header.dispatchEvent(new SheetLoadEvent(e.detail));
                 }
@@ -1076,11 +1334,11 @@ export class ECadViewer extends KCUIElement implements InputContainer {
 window.customElements.define("ecad-viewer", ECadViewer);
 
 class EcadSourceElement extends CustomElement {
-    constructor() {
-        super();
+    override connectedCallback() {
         this.ariaHidden = "true";
         this.hidden = true;
         this.style.display = "none";
+        return super.connectedCallback();
     }
 
     @attribute({ type: String })
@@ -1090,11 +1348,11 @@ class EcadSourceElement extends CustomElement {
 window.customElements.define("ecad-source", EcadSourceElement);
 
 class EcadBlobElement extends CustomElement {
-    constructor() {
-        super();
+    override connectedCallback() {
         this.ariaHidden = "true";
         this.hidden = true;
         this.style.display = "none";
+        return super.connectedCallback();
     }
 
     @attribute({ type: String })
@@ -1107,11 +1365,11 @@ class EcadBlobElement extends CustomElement {
 window.customElements.define("ecad-blob", EcadBlobElement);
 
 class Ov3dElement extends CustomElement {
-    constructor() {
-        super();
+    override connectedCallback() {
         this.ariaHidden = "true";
         this.hidden = true;
         this.style.display = "none";
+        return super.connectedCallback();
     }
 
     @attribute({ type: String })
diff --git a/src/kicad/board.ts b/src/kicad/board.ts
index 0aa3799..5e9321a 100644
--- a/src/kicad/board.ts
+++ b/src/kicad/board.ts
@@ -59,6 +59,18 @@ export class KicadPCB implements BoardNode {
         return this.#net_names.get(idx);
     }
 
+    public findNetNumber(value: string | number): number | null {
+        if (typeof value === "number" && this.#net_names.has(value)) return value;
+        const normalized = String(value ?? "").trim();
+        if (!normalized) return null;
+        const numeric = Number(normalized);
+        if (Number.isInteger(numeric) && this.#net_names.has(numeric)) return numeric;
+        for (const [number, name] of this.#net_names) {
+            if (name === normalized) return number;
+        }
+        return null;
+    }
+
     public getChildren() {
         return [
             ...this.footprints,
@@ -117,7 +129,48 @@ export class KicadPCB implements BoardNode {
                 P.collection("groups", "group", T.item(Group)),
             ),
         );
-        for (const net of this.nets) this.#net_names.set(net.number, net.name);
+        for (const net of this.nets) {
+            if (net.number > 0 && net.name) this.#net_names.set(net.number, net.name);
+        }
+        this.#resolve_name_only_nets();
+    }
+
+    #resolve_name_only_nets() {
+        const names = new Set<string>();
+        for (const segment of this.segments) if (segment.net_name) names.add(segment.net_name);
+        for (const via of this.vias) if (via.net_name) names.add(via.net_name);
+        for (const zone of this.zones) if (zone.net_name) names.add(zone.net_name);
+        for (const footprint of this.footprints) {
+            for (const pad of footprint.pads) if (pad.net?.name) names.add(pad.net.name);
+        }
+
+        const numberByName = new Map<string, number>();
+        for (const [number, name] of this.#net_names) numberByName.set(name, number);
+        let nextNumber = Math.max(0, ...this.#net_names.keys()) + 1;
+        for (const name of [...names].sort()) {
+            if (numberByName.has(name)) continue;
+            numberByName.set(name, nextNumber);
+            this.#net_names.set(nextNumber, name);
+            this.nets.push(Net.fromValues(nextNumber, name));
+            nextNumber += 1;
+        }
+
+        for (const segment of this.segments) {
+            if (segment.net_name) segment.net = numberByName.get(segment.net_name) ?? 0;
+        }
+        for (const via of this.vias) {
+            if (via.net_name) via.net = numberByName.get(via.net_name) ?? 0;
+        }
+        for (const zone of this.zones) {
+            if (zone.net_name) zone.net = numberByName.get(zone.net_name) ?? 0;
+        }
+        for (const footprint of this.footprints) {
+            for (const pad of footprint.pads) {
+                if (pad.net?.name && !pad.net.number) {
+                    pad.net.number = numberByName.get(pad.net.name) ?? 0;
+                }
+            }
+        }
     }
 
     *items() {
@@ -189,6 +242,7 @@ export class LineSegment implements BoardNode {
     width: number;
     layer: string;
     net: number;
+    net_name?: string;
     locked = false;
     tstamp: string;
     uuid?: string;
@@ -222,12 +276,16 @@ export class LineSegment implements BoardNode {
                 P.vec2("end"),
                 P.pair("width", T.number),
                 P.pair("layer", T.string),
-                P.pair("net", T.number),
+                P.pair("net", T.any),
                 P.atom("locked"),
                 P.pair("uuid", T.string),
                 P.pair("tstamp", T.string),
             ),
         );
+        if (typeof this.net === "string") {
+            this.net_name = this.net;
+            this.net = 0;
+        }
     }
 }
 
@@ -239,9 +297,14 @@ export class ArcSegment implements BoardNode {
     width: number;
     layer: string;
     net: number;
+    net_name?: string;
     locked = false;
     tstamp: string;
 
+    get bbox() {
+        return BBox.from_points([this.start, this.mid, this.end]).grow(this.width / 2);
+    }
+
     constructor(expr: Parseable) {
         /*
         (arc
@@ -263,12 +326,16 @@ export class ArcSegment implements BoardNode {
                 P.vec2("end"),
                 P.pair("width", T.number),
                 P.pair("layer", T.string),
-                P.pair("net", T.number),
+                P.pair("net", T.any),
                 P.pair("uuid", T.string),
                 P.atom("locked"),
                 P.pair("tstamp", T.string),
             ),
         );
+        if (typeof this.net === "string") {
+            this.net_name = this.net;
+            this.net = 0;
+        }
     }
 }
 
@@ -284,6 +351,7 @@ export class Via implements BoardNode {
     locked = false;
     free = false;
     net: number;
+    net_name?: string;
     tstamp: string;
     uuid?: string;
 
@@ -308,7 +376,7 @@ export class Via implements BoardNode {
                 P.pair("drill", T.number),
                 P.pair("uuid", T.string),
                 P.list("layers", T.string),
-                P.pair("net", T.number),
+                P.pair("net", T.any),
                 P.atom("locked"),
                 P.atom("free"),
                 P.atom("remove_unused_layers"),
@@ -316,6 +384,10 @@ export class Via implements BoardNode {
                 P.pair("tstamp", T.string),
             ),
         );
+        if (typeof this.net === "string") {
+            this.net_name = this.net;
+            this.net = 0;
+        }
     }
 }
 
@@ -372,7 +444,7 @@ export class Zone implements BoardNode {
                 expr,
                 P.start("zone"),
                 P.atom("locked"),
-                P.pair("net", T.number),
+                P.pair("net", T.any),
                 P.pair("net_name", T.string),
                 P.pair("net_name", T.string),
                 P.pair("name", T.string),
@@ -405,6 +477,10 @@ export class Zone implements BoardNode {
                 P.pair("tstamp", T.string),
             ),
         );
+        if (typeof this.net === "string") {
+            this.net_name ||= this.net;
+            this.net = 0;
+        }
     }
 }
 
@@ -704,15 +780,23 @@ export class Net implements BoardNode {
 
     constructor(expr: Parseable) {
         // (net 2 "+3V3")
-        Object.assign(
-            this,
-            parse_expr(
-                expr,
-                P.start("net"),
-                P.positional("number", T.number),
-                P.positional("name", T.string),
-            ),
+        const parsed = parse_expr(
+            expr,
+            P.start("net"),
+            P.positional("numberOrName", T.any),
+            P.positional("name", T.string),
         );
+        if (typeof parsed["numberOrName"] === "number") {
+            this.number = parsed["numberOrName"];
+            this.name = parsed["name"] ?? "";
+        } else {
+            this.number = 0;
+            this.name = parsed["numberOrName"] ?? parsed["name"] ?? "";
+        }
+    }
+
+    static fromValues(number: number, name: string) {
+        return new Net(["net", number, name]);
     }
 }
 
diff --git a/src/kicanvas/elements/common/app.ts b/src/kicanvas/elements/common/app.ts
index 746f996..cd517c3 100644
--- a/src/kicanvas/elements/common/app.ts
+++ b/src/kicanvas/elements/common/app.ts
@@ -69,6 +69,7 @@ export abstract class KCViewerAppElement<
     #placeholder = html`<ecad-spinner></ecad-spinner>` as HTMLElement;
     #content?: HTMLElement;
     #suppress_next_select_relay = false;
+    public hideSelectionPanel = false;
 
     public set tabMenuHidden(v: boolean) {
         this.#fitter_menu.hidden = v;
@@ -194,8 +195,11 @@ export abstract class KCViewerAppElement<
         );
         this.#viewer_elm = this.make_viewer_element();
         this.#property_viewer = this.make_property_element();
+        const property_viewer = this.hideSelectionPanel
+            ? null
+            : this.#property_viewer;
         return html`
-            ${this.#fitter_menu} ${this.#viewer_elm} ${this.#property_viewer}
+            ${this.#fitter_menu} ${this.#viewer_elm} ${property_viewer}
         `;
     }
 
diff --git a/src/kicanvas/elements/kc-schematic/app.ts b/src/kicanvas/elements/kc-schematic/app.ts
index 51cb674..d423df6 100644
--- a/src/kicanvas/elements/kc-schematic/app.ts
+++ b/src/kicanvas/elements/kc-schematic/app.ts
@@ -62,7 +62,8 @@ export class KCSchematicAppElement extends KCViewerAppElement<KCSchematicViewerE
     override initialContentCallback() {
         super.initialContentCallback();
         this.viewer.addEventListener(SheetChangeEvent.type, (e) => {
-            const sch = this.project.file_by_name(e.detail);
+            if (e.detail.projectPath) this.project.activate_sch(e.detail.projectPath);
+            const sch = this.project.file_by_name(e.detail.filename);
             if (sch instanceof KicadSch) this.viewer.load(sch);
         });
 
diff --git a/src/kicanvas/project.ts b/src/kicanvas/project.ts
index 4e959c2..10631fb 100644
--- a/src/kicanvas/project.ts
+++ b/src/kicanvas/project.ts
@@ -7,7 +7,7 @@
 import { sorted_by_numeric_strings } from "../base/array";
 import { Barrier } from "../base/async";
 import { type IDisposable } from "../base/disposable";
-import { first, length, map } from "../base/iterator";
+import { first, length } from "../base/iterator";
 import { Logger } from "../base/log";
 import { type Constructor } from "../base/types";
 import { KicadPCB, KicadSch, ProjectSettings } from "../kicad";
@@ -18,10 +18,6 @@ import {
 import type { BomItem } from "../kicad/bom_item";
 import { ItemsGroupedByFpValueDNP } from "../kicad/ItemsGroupedByFpValueDNP";
 import { NetRef } from "../kicad/net_ref";
-import type {
-    SchematicSheet,
-    SchematicSheetInstance,
-} from "../kicad/schematic";
 import { SchematicBomVisitor } from "../kicad/schematic_bom_visitor";
 import { NewStrokeGlyph } from "../kicad/text/newstroke-glyphs";
 
@@ -378,21 +374,27 @@ export class Project extends EventTarget implements IDisposable {
 
     public get_first_page(kind: AssertType) {
         switch (kind) {
-            case AssertType.SCH:
+            case AssertType.SCH: {
+                const active = this.active_sch_name
+                    ? this.page_by_path(this.active_sch_name)?.document
+                        ?? this.file_by_name(this.active_sch_name)
+                    : undefined;
                 return (
+                    (active instanceof KicadSch ? active : undefined) ??
                     (this._files_by_name.get(
                         `${this._project_name}.kicad_sch`,
                     ) as KicadSch) ??
                     this.root_schematic_page?.document ??
                     first(this._sch)
                 );
+            }
             case AssertType.PCB:
                 return first(this._pcb);
         }
     }
 
     public page_by_path(project_path: string) {
-        return this._files_by_name.get(project_path);
+        return this._pages_by_path.get(project_path);
     }
 
     public get is_empty() {
@@ -417,87 +419,68 @@ export class Project extends EventTarget implements IDisposable {
     }
 
     _determine_schematic_hierarchy() {
-        const paths_to_schematics = new Map<string, KicadSch>();
-        const paths_to_sheet_instances = new Map<
-            string,
-            { sheet: SchematicSheet; instance: SchematicSheetInstance }
-        >();
-
-        for (const schematic of this.schematics()) {
-            paths_to_schematics.set(`/${schematic.uuid}`, schematic);
-
-            for (const sheet of schematic.sheets) {
-                const sheet_sch = this._files_by_name.get(
-                    sheet.sheetfile ?? "",
-                ) as KicadSch;
-
-                if (!sheet_sch) {
-                    continue;
-                }
-
+        this._pages_by_path.clear();
+        const schematics = Array.from(this.schematics());
+        const normalized = (value: string) => value.replace(/\\/g, "/").replace(/^\.\//, "");
+        const dirname = (value: string) => {
+            const parts = normalized(value).split("/");
+            parts.pop();
+            return parts.join("/");
+        };
+        const resolve_sheet = (owner: KicadSch, reference: string): KicadSch | undefined => {
+            const target = normalized(reference);
+            const owner_dir = dirname(owner.filename);
+            const candidates = [
+                target,
+                owner_dir ? `${owner_dir}/${target}` : target,
+                target.includes("/") ? target : `Subsheets/${target}`,
+            ];
+            for (const candidate of candidates) {
+                const exact = this._files_by_name.get(candidate);
+                if (exact instanceof KicadSch) return exact;
+            }
+            const basename = target.split("/").pop();
+            const matches = schematics.filter((schematic) =>
+                normalized(schematic.filename).split("/").pop() === basename,
+            );
+            return matches.length === 1 ? matches[0] : undefined;
+        };
+
+        const referenced = new Set<KicadSch>();
+        let pages: ProjectPage[] = [];
+        for (const owner of schematics) {
+            for (const sheet of owner.sheets) {
+                const reference = sheet.sheetfile;
+                if (!reference) continue;
+                const target = resolve_sheet(owner, reference);
+                if (!target) continue;
+                referenced.add(target);
                 for (const instance of sheet.instances.values()) {
-                    // paths_to_schematics.set(instance.path, schematic);
-                    paths_to_sheet_instances.set(
-                        `${instance.path}/${sheet.uuid}`,
-                        {
-                            sheet: sheet,
-                            instance: instance,
-                        },
-                    );
+                    const parent_path = String(instance.path || "").replace(/\/$/, "");
+                    pages.push(new ProjectPage(
+                        this,
+                        target.filename,
+                        `${parent_path}/${sheet.uuid}`,
+                        sheet.sheetname ?? reference,
+                        instance.page ?? "",
+                    ));
                 }
             }
         }
 
-        // Find the root sheet. This is done by sorting all of the paths
-        // from shortest to longest and walking through the paths to see if
-        // we can find the schematic for the parent. The first one we find
-        // it the common ancestor (root).
-        const paths = Array.from(paths_to_sheet_instances.keys()).sort(
-            (a, b) => a.length - b.length,
-        );
-
-        let root: KicadSch | undefined;
-        let found_root = false;
-        for (const path of paths) {
-            const parent_path = path.split("/").slice(0, -1).join("/");
-
-            if (!parent_path) {
-                continue;
-            }
-
-            root = paths_to_schematics.get(parent_path);
-
-            if (root) {
-                found_root = true;
-                break;
-            }
-        }
-
-        // If we found a root page, we can build out the list of pages by
-        // walking through paths_to_sheet with root as page one.
-        let pages = [];
-
+        const root = schematics.find((schematic) => schematic.filename === "root.kicad_sch")
+            ?? schematics.find((schematic) => !referenced.has(schematic))
+            ?? first(schematics);
+        const found_root = Boolean(root);
         if (root) {
             this._root_schematic_page = new ProjectPage(
                 this,
                 root.filename,
-                `/${root!.uuid}`,
+                `/${root.uuid}`,
                 "Root",
                 "1",
             );
             pages.push(this._root_schematic_page);
-
-            for (const [path, sheet] of paths_to_sheet_instances.entries()) {
-                pages.push(
-                    new ProjectPage(
-                        this,
-                        sheet.sheet.sheetfile!,
-                        path,
-                        sheet.sheet.sheetname ?? sheet.sheet.sheetfile!,
-                        sheet.instance.page ?? "",
-                    ),
-                );
-            }
         }
 
         // Sort the pages we've collected so far and then insert them
@@ -511,14 +494,13 @@ export class Project extends EventTarget implements IDisposable {
         // Add any "orphan" sheets to the list of pages now that we've added all
         // the hierarchical ones.
         const seen_schematic_files = new Set(
-            map(this._pages_by_path.values(), (p) => p.filename),
+            Array.from(this._pages_by_path.values(), (page) => page.filename),
         );
 
         for (const schematic of this.schematics()) {
             if (!seen_schematic_files.has(schematic.filename)) {
                 const page = new ProjectPage(
                     this,
-                    "schematic",
                     schematic.filename,
                     `/${schematic.uuid}`,
                     schematic.filename,
diff --git a/src/viewers/base/events.ts b/src/viewers/base/events.ts
index e8dd7ad..4ed329a 100644
--- a/src/viewers/base/events.ts
+++ b/src/viewers/base/events.ts
@@ -22,10 +22,26 @@ export class KiCanvasLoadEvent extends KiCanvasEvent<null> {
     }
 }
 
+export interface EcadSemanticSelectionDetail {
+    sourceContext: "SCH" | "PCB";
+    itemType: string;
+    uuid?: string;
+    crossIndex?: string;
+    reference?: string;
+    pin?: string;
+    net?: string;
+    netCode?: number;
+    sheet?: string;
+    page?: string;
+    layer?: string;
+    rawItem?: unknown;
+}
+
 interface SelectDetails {
     item: unknown;
     previous: unknown;
-    sourceContext?: "SCH" | "PCB";
+    sourceContext?: CrossProbeContext;
+    semantic?: EcadSemanticSelectionDetail;
 }
 
 interface SelectedItems {
@@ -150,11 +166,16 @@ export class TabMenuClickEvent extends CustomEvent<TabKind> {
     }
 }
 
-export class SheetChangeEvent extends CustomEvent<string> {
+export interface SheetChangeDetail {
+    filename: string;
+    projectPath?: string;
+}
+
+export class SheetChangeEvent extends CustomEvent<SheetChangeDetail> {
     static readonly type = "kicanvas:sheet:change";
 
-    constructor(fp: string) {
-        super(SheetChangeEvent.type, { detail: fp });
+    constructor(detail: SheetChangeDetail) {
+        super(SheetChangeEvent.type, { detail });
     }
 }
 
@@ -293,7 +314,7 @@ export class PresetUnsetEvent extends CustomEvent<undefined> {
     }
 }
 
-export type CrossProbeContext = "SCH" | "PCB";
+export type CrossProbeContext = "SCH" | "PCB" | "3D" | "BOM";
 export type CrossProbeMode = "hover" | "select" | "focus";
 export type CrossProbeKind = "designator" | "net" | "crossIndex" | "uuid";
 
@@ -307,7 +328,14 @@ export interface CrossProbeRequest {
     page?: string;
     designator?: string;
     net?: string;
+    netCode?: number;
+    pin?: string;
     crossIndex?: string;
+    uuid?: string;
+    uuids?: string[];
+    componentUid?: string;
+    netUid?: string;
+    terminalUid?: string;
 }
 
 export interface CrossProbeTargetHint {
@@ -318,6 +346,10 @@ export interface CrossProbeTargetHint {
     net?: string;
     crossIndex?: string;
     uuid?: string;
+    uuids?: string[];
+    netCode?: number;
+    pin?: string;
+    mode?: CrossProbeMode;
 }
 
 export type CrossProbeFailureReason =
diff --git a/src/viewers/board/footprint-painter.ts b/src/viewers/board/footprint-painter.ts
index c760efe..4449c64 100644
--- a/src/viewers/board/footprint-painter.ts
+++ b/src/viewers/board/footprint-painter.ts
@@ -75,6 +75,20 @@ export class FootprintPainter extends BoardItemPainter {
             return;
         }
 
+        if (layer.name === ViewLayerNames.selection_fg && this.filter_net) {
+            const matrix = Matrix3.translation(
+                fp.at.position.x,
+                fp.at.position.y,
+            ).rotate_self(Angle.deg_to_rad(fp.at.rotation));
+            this.gfx.state.push();
+            this.gfx.state.multiply(matrix);
+            for (const pad of fp.pads) {
+                if (pad.net?.number === this.filter_net) this.view_painter.paint_item(layer, pad);
+            }
+            this.gfx.state.pop();
+            return;
+        }
+
         const matrix = Matrix3.translation(
             fp.at.position.x,
             fp.at.position.y,
diff --git a/src/viewers/board/painter.ts b/src/viewers/board/painter.ts
index df83121..1b2c7ab 100644
--- a/src/viewers/board/painter.ts
+++ b/src/viewers/board/painter.ts
@@ -791,14 +791,16 @@ export class BoardPainter extends DocumentPainter {
             for (const item of board.items()) {
                 switch (item.typeId) {
                     case "LineSegment":
-                        if ((item as board_items.LineSegment).net === net) {
+                    case "ArcSegment":
+                    case "Via":
+                        if ((item as board_items.LineSegment | board_items.ArcSegment | board_items.Via).net === net) {
                             const painter = this.painter_for(item);
-                            const line = item as board_items.LineSegment;
+                            const netItem = item as board_items.LineSegment | board_items.ArcSegment | board_items.Via;
 
-                            if (!this.#net_bbox) this.#net_bbox = line.bbox;
+                            if (!this.#net_bbox) this.#net_bbox = netItem.bbox;
                             else
                                 this.#net_bbox = BBox.combine([
-                                    line.bbox,
+                                    netItem.bbox,
                                     this.#net_bbox,
                                 ]);
 
@@ -808,14 +810,29 @@ export class BoardPainter extends DocumentPainter {
                         break;
                     case "Zone":
                         {
+                            const zone = item as board_items.Zone;
+                            if (zone.net !== net) break;
                             const painter = this.painter_for(item);
 
                             if (!painter) continue;
 
                             this.paint_item(layer, item);
+                            if (!this.#net_bbox) this.#net_bbox = zone.bbox;
+                            else this.#net_bbox = BBox.combine([zone.bbox, this.#net_bbox]);
                         }
                         break;
 
+                    case "Footprint": {
+                        const footprint = item as board_items.Footprint;
+                        if (!footprint.pads.some((pad) => pad.net?.number === net)) break;
+                        const painter = this.painter_for(item);
+                        if (!painter) break;
+                        this.paint_item(layer, item);
+                        if (!this.#net_bbox) this.#net_bbox = footprint.bbox;
+                        else this.#net_bbox = BBox.combine([footprint.bbox, this.#net_bbox]);
+                        break;
+                    }
+
                     default:
                         break;
                 }
diff --git a/src/viewers/board/viewer.ts b/src/viewers/board/viewer.ts
index b3b24ce..eb8cf78 100644
--- a/src/viewers/board/viewer.ts
+++ b/src/viewers/board/viewer.ts
@@ -39,6 +39,7 @@ export class BoardViewer extends DocumentViewer<
     #should_restore_visibility = false;
     #zones_visibility = new Map<string, VisibilityType>();
     #layer_visibility_ctrl: KCBoardLayersPanelElement;
+    #cross_probe_highlight_active = false;
 
     set layer_visibility_ctrl(ctr: KCBoardLayersPanelElement) {
         this.#layer_visibility_ctrl = ctr;
@@ -47,12 +48,8 @@ export class BoardViewer extends DocumentViewer<
         this.#layer_visibility_ctrl.clear_highlight();
         if (this.painter.paint_net(this.board, num, this.layer_visibility)) {
             this.#should_restore_visibility = false;
-            if (num) {
-                this.#should_restore_visibility = true;
-                for (const layer of this.layers.in_ui_order()) {
-                    layer.visible = false;
-                }
-            }
+            this.layers.selection_bg.opacity = 0.58;
+            this.layers.selection_fg.opacity = 1;
             this.draw();
         }
         if (num) {
@@ -86,6 +83,16 @@ export class BoardViewer extends DocumentViewer<
             this.#zones_visibility.clear();
             this.draw();
         }
+
+        if (this.painter.filter_net) this.clear_highlight();
+    }
+
+    public clear_highlight() {
+        this.#cross_probe_highlight_active = false;
+        this.#should_restore_visibility = false;
+        this.painter.filter_net = null;
+        this.painter.clear_interactive();
+        this.draw();
     }
 
     public highlight_fp(fp: board_items.Footprint) {
@@ -108,6 +115,7 @@ export class BoardViewer extends DocumentViewer<
     }
 
     public focus_footprint(fp: board_items.Footprint) {
+        this.#cross_probe_highlight_active = true;
         if (this.#should_restore_visibility) {
             const visibilities = this.layer_visibility;
             for (const layer of this.layers.in_ui_order()) {
@@ -131,6 +139,7 @@ export class BoardViewer extends DocumentViewer<
     }
 
     public focus_net(num: number | null) {
+        this.#cross_probe_highlight_active = true;
         this.highlight_net(num);
         const net_bbox = this.painter.net_bbox;
         if (net_bbox) {
@@ -141,36 +150,56 @@ export class BoardViewer extends DocumentViewer<
         }
     }
 
+    public focus_uuid(uuid: string): boolean {
+        for (const [, items] of this.#interactive) {
+            const match = items.find((entry) => {
+                const item = entry.item as any;
+                return item?.uuid === uuid || item?.tstamp === uuid;
+            });
+            if (!match) continue;
+            const bbox = (match as any).bbox;
+            if (!bbox) continue;
+            this.painter.filter_net = null;
+            this.#cross_probe_highlight_active = true;
+            this.painter.clear_interactive();
+            this.painter.highlight(match);
+            this.viewport.camera.bbox = bbox.grow(
+                Math.max(bbox.w * 0.8, 2),
+                Math.max(bbox.h * 0.8, 2),
+            );
+            this.draw();
+            return true;
+        }
+        return false;
+    }
+
     override on_click(pos: Vec2, event?: MouseEvent): void {
         const items = this.find_items_under_pos(pos);
 
         // In comment mode, dispatch CommentClickEvent with element info
         if (this.commentModeEnabled && event) {
-            // Only dispatch if we found an element
-            if (items.length > 0) {
-                const it = items[0]!;
-                const item = it.item as any;
-
-                // const rect = this.canvas.getBoundingClientRect();
-                this.dispatchEvent(
-                    new CommentClickEvent({
-                        worldX: pos.x,
-                        worldY: pos.y,
-                        screenX: event.clientX,
-                        screenY: event.clientY,
-                        layer: it.is_on_layer?.("F.Cu") ? "F.Cu" : "B.Cu",
-                        context: "PCB",
-                        elementType: item?.typeId || "Unknown",
-                        elementId: item?.uuid || "",
-                        elementRef: item?.reference || item?.designator || "",
-                        element: item,
-                    }),
-                );
-            }
+            const hit = items[0];
+            const item = hit?.item as any;
+            this.dispatchEvent(
+                new CommentClickEvent({
+                    worldX: pos.x,
+                    worldY: pos.y,
+                    screenX: event.clientX,
+                    screenY: event.clientY,
+                    layer: hit?.is_on_layer?.("F.Cu") ? "F.Cu" : "B.Cu",
+                    context: "PCB",
+                    elementType: item?.typeId || "Whitespace",
+                    elementId: item?.uuid || item?.tstamp || "",
+                    elementRef: item?.reference || item?.designator || "",
+                    element: item ?? null,
+                }),
+            );
             // Don't dispatch select event in comment mode
             return;
         }
 
+        this.#cross_probe_highlight_active = false;
+
         // Normal mode - dispatch selection events
         if (items.length > 0) {
             if (items.length == 1) {
@@ -407,6 +436,7 @@ export class BoardViewer extends DocumentViewer<
     }
 
     override on_hover(_pos: Vec2) {
+        if (this.#cross_probe_highlight_active) return;
         const hover_item = this.findInteractive(_pos);
 
         if (hover_item === this.#last_hover) return;
diff --git a/src/viewers/schematic/painters/label.ts b/src/viewers/schematic/painters/label.ts
index 3e20c1a..5f7450f 100644
--- a/src/viewers/schematic/painters/label.ts
+++ b/src/viewers/schematic/painters/label.ts
@@ -4,8 +4,8 @@
     Full text available at: https://opensource.org/licenses/MIT
 */
 
-import { Color } from "../../../base/color";
 import { Angle, Vec2 } from "../../../base/math";
+import { Color, Polyline } from "../../../graphics";
 import * as schematic_items from "../../../kicad/schematic";
 import { SchText, StrokeFont } from "../../../kicad/text";
 import { LayerNames, ViewLayer } from "../layers";
@@ -28,7 +28,7 @@ import { SchematicItemPainter } from "./base";
 
 export abstract class LabelPainter extends SchematicItemPainter {
     override layers_for(item: HierarchicalLabelPainter) {
-        return [LayerNames.label];
+        return [LayerNames.label, LayerNames.interactive];
     }
     override paint(layer: ViewLayer, l: schematic_items.Label) {
         if (l.effects.hide) {
@@ -51,6 +51,26 @@ export abstract class LabelPainter extends SchematicItemPainter {
             this.get_schematic_text_offset(l, schtext),
         );
 
+        if (layer.name === LayerNames.interactive) {
+            const width = Math.max(schtext.text_width / 10000, 0.8);
+            const height = Math.max(schtext.text_size.y / 10000, 0.8);
+            const anchor = l.at.position;
+            this.gfx.line(
+                new Polyline(
+                    [
+                        new Vec2(anchor.x - width / 2, anchor.y - height / 2),
+                        new Vec2(anchor.x + width / 2, anchor.y - height / 2),
+                        new Vec2(anchor.x + width / 2, anchor.y + height / 2),
+                        new Vec2(anchor.x - width / 2, anchor.y + height / 2),
+                        new Vec2(anchor.x - width / 2, anchor.y - height / 2),
+                    ],
+                    0.1,
+                    Color.white,
+                ),
+            );
+            return;
+        }
+
         this.gfx.state.push();
         this.gfx.state.stroke = this.color;
         this.gfx.state.fill = this.color;
diff --git a/src/viewers/schematic/viewer.ts b/src/viewers/schematic/viewer.ts
index a8e9651..fdee298 100644
--- a/src/viewers/schematic/viewer.ts
+++ b/src/viewers/schematic/viewer.ts
@@ -156,7 +156,12 @@ export class SchematicViewer extends DocumentViewer<
         if (this.document.sheets)
             for (const item of this.document.sheets) {
                 if (item.bbox.contains_point(pos) && item.sheetfile) {
-                    this.dispatchEvent(new SheetChangeEvent(item.sheetfile));
+                    this.dispatchEvent(new SheetChangeEvent({
+                        filename: item.sheetfile,
+                        projectPath: item.path
+                            ? `${item.sheetfile}:${item.path}/${item.uuid}`
+                            : undefined,
+                    }));
                     break;
                 }
             }
@@ -240,6 +245,29 @@ export class SchematicViewer extends DocumentViewer<
         }
     }
 
+    public highlight_uuids(uuids: string[], focus = false): boolean {
+        const bboxes = uuids
+            .map((uuid) => this.schematic_renderer.get_item_bbox(uuid))
+            .filter((bbox): bbox is BBox => Boolean(bbox));
+        if (!bboxes.length) {
+            this.paint_selected_bboxes([]);
+            return false;
+        }
+        if (focus) {
+            const combined = BBox.combine(bboxes);
+            this.viewport.camera.bbox = combined.grow(
+                Math.max(combined.w * 0.2, 5),
+                Math.max(combined.h * 0.2, 5),
+            );
+        }
+        this.paint_selected_bboxes(bboxes);
+        return true;
+    }
+
+    public clear_highlight() {
+        this.paint_selected_bboxes([]);
+    }
+
     protected override create_painter() {
         return new SchematicPainter(this.renderer, this.layers, this.theme);
     }
@@ -249,23 +277,28 @@ export class SchematicViewer extends DocumentViewer<
     }
 
     protected paint_selected(selected: BBox | null) {
+        this.paint_selected_bboxes(selected ? [selected] : []);
+    }
+
+    protected paint_selected_bboxes(selected: BBox[]) {
         const layer = this.layers.selection_bg;
 
         layer.clear();
 
-        if (selected) {
+        if (selected.length) {
             const color = new Color(0.1, 0.2, 1, 0.2);
             this.renderer.start_layer(layer.name);
 
-            this.renderer.line(
-                Polyline.from_BBox(
-                    selected,
-                    SchematicViewer.InterActiveBBoxLineWidth,
-                    color,
-                ),
-            );
-
-            this.renderer.polygon(Polygon.from_BBox(selected, color));
+            for (const bbox of selected) {
+                this.renderer.line(
+                    Polyline.from_BBox(
+                        bbox,
+                        SchematicViewer.InterActiveBBoxLineWidth,
+                        color,
+                    ),
+                );
+                this.renderer.polygon(Polygon.from_BBox(bbox, color));
+            }
 
             layer.graphics = this.renderer.end_layer();
 
